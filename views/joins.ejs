<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>JOIN запити - Thing Exchange ORM</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">Thing Exchange ORM</a>
            <div class="navbar-nav">
                <a class="nav-link" href="/">Головна</a>
                <a class="nav-link active" href="/joins">JOIN запити</a>
                <a class="nav-link" href="/aggregates">Агрегатні запити</a>
                <a class="nav-link" href="/groupby">GROUP BY + HAVING</a>
                <a class="nav-link" href="/users">Користувачі</a>
            </div>
        </div>
    </nav>
    
    <div class="container mt-4">
        <h1>JOIN запити</h1>
        
        <h3>1. Користувачі з їх оголошеннями (LEFT JOIN)</h3>
        <div class="table-responsive">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Користувач</th>
                        <th>Email</th>
                        <th>Кількість оголошень</th>
                    </tr>
                </thead>
                <tbody>
                    <% usersWithListings.forEach(user => { %>
                    <tr>
                        <td><%= user.id %></td>
                        <td><%= user.firstName %> <%= user.secondName %></td>
                        <td><%= user.email %></td>
                        <td><%= user.listings ? user.listings.length : 0 %></td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
        
        <h3 class="mt-4">2. Оголошення з діями модерації (LEFT JOIN)</h3>
        <div class="table-responsive">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Заголовок</th>
                        <th>Автор</th>
                        <th>Дії модерації</th>
                    </tr>
                </thead>
                <tbody>
                    <% listingsWithActions.forEach(listing => { %>
                    <tr>
                        <td><%= listing.id %></td>
                        <td><%= listing.header %></td>
                        <td><%= listing.user.firstName %> <%= listing.user.secondName %></td>
                        <td><%= listing.moderationActions ? listing.moderationActions.length : 0 %></td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>