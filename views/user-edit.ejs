<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Редагувати <%= user.username %> - Thing Exchange ORM</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">Thing Exchange ORM</a>
            <div class="navbar-nav">
                <a class="nav-link" href="/">Головна</a>
                <a class="nav-link" href="/joins">JOIN запити</a>
                <a class="nav-link" href="/aggregates">Агрегатні запити</a>
                <a class="nav-link" href="/groupby">GROUP BY + HAVING</a>
                <a class="nav-link active" href="/users">Користувачі</a>
            </div>
        </div>
    </nav>
    
    <div class="container mt-4">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header">
                        <h4>Редагувати користувача: <%= user.username %></h4>
                    </div>
                    <div class="card-body">
                        <form action="/users/<%= user.id %>/update" method="POST">
                            <div class="row">
                                <div class="col-md-6">
                                    <div class="mb-3">
                                        <label class="form-label">Username</label>
                                        <input type="text" class="form-control" value="<%= user.username %>" readonly>
                                        <small class="form-text text-muted">Username не можна змінити</small>
                                    </div>
                                    
                                    <div class="mb-3">
                                        <label for="firstName" class="form-label">Ім'я *</label>
                                        <input type="text" class="form-control" id="firstName" name="firstName" value="<%= user.firstName %>" required>
                                    </div>
                                    
                                    <div class="mb-3">
                                        <label for="secondName" class="form-label">Прізвище *</label>
                                        <input type="text" class="form-control" id="secondName" name="secondName" value="<%= user.secondName %>" required>
                                    </div>
                                </div>
                                
                                <div class="col-md-6">
                                    <div class="mb-3">
                                        <label for="email" class="form-label">Email *</label>
                                        <input type="email" class="form-control" id="email" name="email" value="<%= user.email %>" required>
                                    </div>
                                    
                                    <div class="mb-3">
                                        <label class="form-label">Роль</label>
                                        <input type="text" class="form-control" value="<%= user.role %>" readonly>
                                        <small class="form-text text-muted">Роль не можна змінити через цю форму</small>
                                    </div>
                                    
                                    <div class="mb-3">
                                        <label for="balance" class="form-label">Баланс (₴)</label>
                                        <input type="number" step="0.01" class="form-control" id="balance" name="balance" value="<%= user.balance %>">
                                    </div>
                                </div>
                            </div>
                            
                            <div class="mb-3">
                                <label class="form-label">Статус</label>
                                <div>
                                    <span class="badge bg-<%= user.status === 'active' ? 'success' : 'secondary' %>"><%= user.status %></span>
                                    <small class="form-text text-muted ms-2">Статус керується окремо</small>
                                </div>
                            </div>
                            
                            <div class="d-flex justify-content-between">
                                <div>
                                    <a href="/users/<%= user.id %>" class="btn btn-secondary">Скасувати</a>
                                    <a href="/users" class="btn btn-outline-secondary">До списку</a>
                                </div>
                                <button type="submit" class="btn btn-primary">Зберегти зміни</button>
                            </div>
                        </form>
                    </div>
                </div>
                
                <div class="card mt-3">
                    <div class="card-header bg-warning">
                        <h6 class="mb-0">Додаткові дії</h6>
                    </div>
                    <div class="card-body">
                        <div class="d-flex gap-2">
                            <a href="/users/<%= user.id %>/delete-confirm" class="btn btn-danger btn-sm">Видалити користувача</a>
                            <button class="btn btn-warning btn-sm" disabled>Змінити статус</button>
                            <button class="btn btn-info btn-sm" disabled>Змінити роль</button>
                        </div>
                        <small class="form-text text-muted">Деякі функції ще не реалізовані</small>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>