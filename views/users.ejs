<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Користувачі - Thing Exchange ORM</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">Thing Exchange ORM</a>
            <div class="navbar-nav">
                <a class="nav-link" href="/">Головна</a>
                <a class="nav-link" href="/joins">JOIN запити</a>
                <a class="nav-link" href="/aggregates">Агрегатні запити</a>
                <a class="nav-link" href="/groupby">GROUP BY + HAVING</a>
                <a class="nav-link active" href="/users">Користувачі</a>
            </div>
        </div>
    </nav>
    
    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center mb-3">
            <h2>Користувачі системи</h2>
            <a href="/users/new" class="btn btn-success">
                ➕ Створити користувача
            </a>
        </div>
        
        <div class="table-responsive">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Username</th>
                        <th>Ім'я</th>
                        <th>Прізвище</th>
                        <th>Email</th>
                        <th>Роль</th>
                        <th>Статус</th>
                        <th>Баланс</th>
                        <th>Дії</th>
                    </tr>
                </thead>
                <tbody>
                    <% users.forEach(user => { %>
                    <tr>
                        <td><%= user.id %></td>
                        <td><%= user.username %></td>
                        <td><%= user.firstName %></td>
                        <td><%= user.secondName %></td>
                        <td><%= user.email %></td>
                        <td><span class="badge bg-<%= user.role === 'admin' ? 'danger' : user.role === 'moderator' ? 'warning' : 'primary' %>"><%= user.role %></span></td>
                        <td><span class="badge bg-<%= user.status === 'active' ? 'success' : 'secondary' %>"><%= user.status %></span></td>
                        <td>₴<%= user.balance %></td>
                        <td>
                            <div class="btn-group btn-group-sm">
                                <a href="/users/<%= user.id %>" class="btn btn-primary">Переглянути</a>
                                <a href="/users/<%= user.id %>/edit" class="btn btn-warning">Редагувати</a>
                                <a href="/users/<%= user.id %>/delete-confirm" class="btn btn-danger">Видалити</a>
                            </div>
                        </td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>